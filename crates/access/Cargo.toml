[package]
name = "access"
version = "0.1.0"
edition = "2024"

[dependencies]
async-trait = "0.1"
schemars = { version = "1", features = ["uuid1"] }
serde = { version = "1", features = ["derive"] }
thiserror = "1"
uuid = { version = "1", features = ["serde"] }

[dev-dependencies]
serde_json = "1"
