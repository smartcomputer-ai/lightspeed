[package]
name = "profiles"
version = "0.1.0"
edition = "2024"

[dependencies]
api = { path = "../api" }
async-trait = "0.1"
environment-protocol = { path = "../environment-protocol" }
thiserror = "1"

[dev-dependencies]
serde_json = "1"
